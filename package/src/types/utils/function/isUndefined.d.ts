/**
 * The function `isUndefined` in TypeScript checks if a value is undefined and returns a boolean
 * result.
 * @param {unknown} value - The `value` parameter in the `isUndefined` function is of type `unknown`,
 * which means it can be any type. The function checks if the type of the `value` is "undefined" and
 * returns a boolean value accordingly.
 * @returns The function `isUndefined` is returning a boolean value indicating whether the input
 * `value` is of type "undefined".
 */
declare function isUndefined(value: unknown): boolean;
