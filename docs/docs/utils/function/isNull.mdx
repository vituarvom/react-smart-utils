---
title: "isNull Function Documentation"
---

import { isNull } from './isNull';

# `isNull` Utility Function

The `isNull` function checks if a given value is exactly `null`. It returns `true` only when the value is strictly `null` and `false` for any other value.

## Function Definition

```typescript
function isNull(value: any): boolean {
    return value === null;
}
